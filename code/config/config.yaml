experiment: 1
logger:
  folder_path: runs
  wandb_project: HRL
model:
  path: models
  load_model: null
defaults:
  - env: fourrooms
  - agent: a2c
  - _self_

hydra:
  sweeper:
    params:
      experiment: range(50)
hydra:
  launcher:
    n_jobs: 5
  sweeper:
    params:
      experiment: range(11, 21)

experiment: 11
note: null
replicate_experiment: null
logger:
  type: base
  folder_path: logs
  wandb_project: HRL
  log_step: False
  log_episode: True
  log_terminal: True

model:
  path: models
  load_model: null

defaults:
  - env: breakout
  - agent: oc
  - override hydra/launcher: joblib
  - _self_


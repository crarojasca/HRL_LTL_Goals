hydra:
  launcher:
    n_jobs: 5
  sweeper:
    params:
      experiment: range(6, 11)

experiment: 1
note: null
replicate_experiment: fourrooms_transfer_experiment
logger:
  type: base
  folder_path: logs
  wandb_project: HRL
  log_step: False
  log_episode: True
  log_terminal: True

model:
  path: models
  load_model: null

defaults:
  - env: fourrooms
  - agent: oc
  - override hydra/launcher: joblib
  - _self_

